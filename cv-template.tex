\documentclass[10pt,letterpaper]{article}
\usepackage[margin=0.75in]{geometry}
\usepackage{fontspec}
\usepackage{titlesec}
\usepackage{hyperref}

% Clear default page numbers
\pagestyle{empty}

% Suppress all section/subsection numbering
\setcounter{secnumdepth}{0}

% Modern sans-serif font
\setmainfont{Inter}[Ligatures=TeX]

% Setup custom scannable headings with horizontal rules
\titleformat{\subsection}{\large\bfseries}{}{0em}{}[\titlerule]
\titlespacing{\subsection}{0pt}{14pt}{6pt}

% Force contact line to fit on one line without wrapping
\newcommand{\contact}[1]{\vspace{2pt}\par\noindent\hbox to \textwidth{\small #1}\par\vspace{2pt}}

% Fix for Pandoc's automatic tight list macro
\providecommand{\tightlist}{%
  \setlength{\itemsep}{2pt}\setlength{\parskip}{1pt}}

\newcommand{\nametitle}[1]{{\Huge\bfseries \noindent #1}\par\vspace{4pt}}

\begin{document}

% Pandoc will automatically inject your parsed markdown file right here
$body$

\end{document}